# Reconstruction validation report

Date: 2026-09-13 UTC

## Inventory

- Root files restored from preserved sources: **421**.
- Historical divergence records: **534**.
- Unique paths with divergent historical variants: **64**.
- Files in the reconstructed operational tree, excluding `preservation/` and
  Git internals: **460** at validation time.

No existing root path was overwritten. All alternative versions remain under
`preservation/branch-snapshots/` and are addressable through their SHA-256
records in `ROOT_UNION_CONFLICTS.tsv`.

## Tests executed

- `python3 -m unittest discover -s tests -v`: **44/44 passed**.
- `python3 test_g1_formalization.py`: passed static audit; no `sorry`, `admit`,
  `axiom`, or `unsafe` token in the audited target; unconditional theorem is
  correctly reported as absent.
- `python3 scripts/g1_componentwise_ch_test.py`: exited successfully and remains
  classified as `synthetic_falsification_only`.
- `python3 scripts/g1_joint_tail_diagnostic.py`: exited successfully and remains
  classified as an empirical proxy, not an analytic theorem.
- `python3 python/forcing_test.py`: exited successfully.

`pytest`, Lean, Coq, Agda and Isabelle executables are unavailable in this
runtime. Their kernel checks must therefore be performed by an environment or
CI runner that installs the declared toolchains. This report does not promote
any unexecuted formal target to verified status.

`git diff --check` reports pre-existing trailing whitespace and blank lines in
historical Markdown/CSV artifacts. These files were intentionally not rewritten:
normalizing them would change the archived evidence rather than reconstruct it
faithfully. Content integrity is instead checked by the recorded SHA-256 values.

## Generated-cache handling

Python changed a tracked bytecode cache during testing. The generated bytecode
was copied to `reconstruction/test-generated/`; the tracked original was then
restored byte-for-byte from the pre-reconstruction `main` object. No generated
test side effect is silently substituted for a historical file.
